import { Syncopate, Sacramento } from "next/font/google";

export const syncopate = Syncopate({
  weight: ["400", "700"],
  subsets: ["latin"],
});

export const sacramento = Sacramento({ weight: ["400"], subsets: ["latin"] });
